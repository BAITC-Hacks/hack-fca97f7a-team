# Copy to .env on the server; never put a secret in frontend/.env.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini
SUMMARY_BACKEND=llm
DATA_MODE=fixture
# Optional local operator-reviewed evidence file; leave unset until real as-issued captures exist.
# See CONTRACTS.md. Never substitute retrospective queries or fixture evidence.
OPEN_METEO_ARCHIVE_MANIFEST=
