# Текущий результат: настоящая погода подключена

- [x] `live` по умолчанию: Open-Meteo → проверенный CSV → модель.
- [x] Живой запрос для T1/T2: по 48 часов успешно получены и обработаны моделями.
- [x] Дата определяется сервером; ошибки API не заменяются синтетикой.
- [ ] A: исторические свидетельства и февральское покрытие архива.
- [ ] B: проверить соответствие ветра 10 м обучающим признакам и качество прогноза.
- [ ] C: финальная пользовательская репетиция с live-погодой.

# Задачи команды: демо прогноза мощности ВЭС

Цель: пользователь выбирает турбину → получает прогноз погоды → сервер создаёт
CSV → модель читает CSV и прогнозирует мощность → пользователь видит таблицу,
график и объяснение на русском языке → задаёт вопросы по этому прогнозу.

В команде три разработчика: A — backend и погода, B — данные и модель,
C — frontend и объяснения. Имена можно подставить вместо букв.
Контракты модулей: [CONTRACTS.md](CONTRACTS.md). Запуск: [README.md](README.md).

## Статус main и ближайшие действия

Модель из `MLmodel` интегрирована в main коммитом `f5b38ea`: CSV-вход,
загрузка по ID турбины и метаданные совместимы с React/FastAPI. Проверены
155 тестов и сборка React после интеграции модели и устойчивости демо.
Ранее проверены 8 сценариев CSV-инференса. Перевод уже в main.
Адаптер `dev-a` интегрирован: Open-Meteo Single Runs, проверка внешнего
свидетельства доступности, контрольные суммы и сохранение сырой погоды.
Транспорт реализован; реальный архив по-прежнему заблокирован отсутствием
проверенного реестра выпусков. Это не завершение A4.

Редизайн из `c-minimal-interface` (`72929d4`) также интегрирован: компактная
карта, русский интерфейс, чат по прогнозу, экспорт графика PNG/SVG и координаты
T1/T2, предоставленные пользователем. По умолчанию используется настоящий текущий прогноз Open-Meteo; fixtures доступны отдельно.

| Разработчик | Делать сейчас | Ожидаемый результат |
|---|---|---|
| A | A2–A4: получить независимые свидетельства выпусков, настроить реестр и проверить реальную погоду | Проверенный погодный набор → существующий CSV → модель; явные ошибки |
| B | B1: оценить качество на as-issued погоде; выполнить B4 после A4 | Подтверждённая оценка качества; полный февральский результат |
| C | C6: провести общую репетицию после подключения реальной погоды | Русская демка проходит основной и аварийные сценарии |

B2 и B3 уже выполнены — повторно писать обучение и январскую диагностику не нужно.
CLI replay уже реализован и проверен на fixtures; реальная февральская
сдача блокируется A4. Оценка февраля B5 блокируется отсутствием истины.

## Что уже реализовано

- [x] React + FastAPI, выбор T1/T2, график и таблица на 24/48 часов.
- [x] Погода → проверенный CSV → чтение этого файла моделью → прогноз.
- [x] Отдельные модели, обученные на реальных данных каждой турбины.
- [x] Версионированные артефакты, контрольные суммы, реестр `latest.json` и
  совместимость загрузки старых локальных моделей до переобучения.
- [x] Январский хронологический holdout, MAE/RMSE/R² и baseline,
  обновляемый в каждый origin, для T1/T2 и 24/48 часов.
- [x] Скачивание прогноза и точного входного CSV модели.
- [x] OpenAI-объяснение и вопросы по сохранённому на сервере прогнозу.
- [x] Расчётное объяснение при недоступности LLM; сравнение соседних запусков.
- [x] Проверки контрактов, сборка React и один успешный живой тест OpenAI.
- [x] Минималистичный редизайн, чат в рамках текущего прогноза, экспорт PNG/SVG.
- [x] Координаты T1/T2 от пользователя и ссылки на источник в реестре и интерфейсе.

Live использует реальный Open-Meteo ECMWF IFS; fixture-погода синтетическая. Координаты T1/T2 предоставлены пользователем и
добавлены со ссылками на источник. Русский минималистичный интерфейс проверен
в браузере, включая выгрузки CSV/PNG/SVG. Мощность нормализована в диапазоне [0,1]; перевод в МВт/МВт·ч
невозможен без подтверждённых данных о мощности и нормализации.

## Приоритеты

- **P0 — обязательное демо:** полностью русский пользовательский сценарий,
  проверенные границы модулей, понятные ошибки и воспроизводимый запуск.
- **P1 — реальная погода:** подтверждённые координаты и допустимый погодный
  источник вместо заглушек. При отсутствии доступа демо остаётся явно
  демонстрационным; задачу нельзя считать завершённой.
- **P2 — полное решение задания:** исторические прогнозы погоды, пакетный
  февральский расчёт и проверка качества. Это обязательно для полной сдачи,
  даже если презентацию проводят раньше.

## Разработчик A — FastAPI, оркестрация и погода

Владеет `api.py`, `agent.py`, `weather.py`, `contracts.py`, конфигурацией
провайдера и соответствующими тестами. Координирует общие зависимости.

- [ ] **A1 / P0: ошибки и статусы для русского демо.** Согласовать с C стабильные
  коды ошибок и русские сообщения для отсутствующей погоды, неверной даты,
  недоступного сервиса, потерянного после перезапуска прогноза и повреждённого
  CSV. Не показывать пользователю traceback или сырые ответы провайдера.
  **Готово:** каждый сценарий имеет понятное сообщение и действие для повтора;
  HTTP-коды и программные идентификаторы остаются стабильными.
- [ ] **A2 / P1: подтвердить метаданные турбин и источник погоды.** Получить
  координаты T1/T2, часовой пояс, доступность почасовых 24/48 часов и права
  доступа к API. Выбрать источник после проверки покрытия; название OpenWeather
  само по себе не гарантирует нужный архив или горизонт.
  **Статус:** координаты пользователя уже интегрированы (`user_provided`);
  остаются выбор провайдера, его покрытие и подтверждение погодных метаданных.
  **Готово:** источник и ограничения записаны, координаты имеют подтверждение.
  **Прогресс:** пользователь подтвердил координаты и соответствие T1/T2;
  точки и исходные ссылки внесены в `weather.py` и `CONTRACTS.md`.
  Выбран адаптер Open-Meteo Single Runs; исследование покрытия описано в
  docs/open-meteo-verification.md. Подтверждение исторической доступности
  конкретных выпусков остаётся открытым.
- [ ] **A3 / P1: завершить приёмку погодного адаптера.** Реализация и offline-тесты готовы;
  остаётся реальный сквозной запуск с проверенным реестром. Сохранить интерфейс
  `fetch_weather`, привести данные к UTC, м/с и °C, проверить полноту часов,
  добавить таймаут, обработку лимита/ошибки API и происхождение данных.
  Ключ хранить только на сервере. Заглушка выбирается явно, без тихого перехода.
  **Готово:** обе турбины проходят цепочку до CSV; неполные и некорректные
  ответы покрыты тестами на сохранённых/поддельных ответах API.
- [ ] **A4 / P2: историческая погода на момент выпуска.** Для каждого origin
  получать именно доступный тогда прогноз; сохранять исходный ответ, контрольную
  сумму и доказательство времени доступности. Проверять
  `initialized_at <= available_at <= origin`.
  **Готово:** B получает проверенные погодные наборы для всех февральских запусков.
  Фактическая погода, реанализ и прогноз, выпущенный позже origin, недопустимы.

**Важно:** текущий контракт поддерживает `live`, `fixture` и `archive`.
Live выдаёт текущую погоду и явно помеченную экспериментальную модель.
Нельзя подставлять сегодняшнюю погоду под январскую
дату или снимать проверки исторической доступности.

## Разработчик B — данные, CSV, прогноз и качество

Владеет `data.py`, `model_input.py`, `model.py`, `scripts/train.py`, будущими
скриптами оценки/пакетного расчёта и соответствующими тестами.

- [ ] **B1 / P0: проверить совместимость реальной погоды и модели.** С A
  подтвердить значение скорости ветра (включая высоту измерения/прогноза),
  температуру, единицы, время и порядок признаков. Описать расхождения между
  измеренными признаками обучения и прогнозными признаками инференса.
  **Готово:** модель по-прежнему читает скачиваемый CSV, выдаёт ровно 24/48
  конечных значений в правильном порядке; ограничения записаны.
- [x] **B2 / P0: воспроизводимое обучение.** Проверить создание обеих моделей
  из исходных CSV одной документированной командой, идентичность турбины,
  метаданные, исключённые интервалы и соблюдение временной отсечки.
  **Готово:** запуск после удаления только генерируемых артефактов воспроизводит
  рабочее демо; обучение не выполняется внутри HTTP-запроса.
- [x] **B3 / P1: честная оценка на историческом holdout.** Разделить данные
  хронологически до февраля, сравнить модель с простым baseline, посчитать
  MAE/RMSE отдельно для T1/T2. Не подбирать модель по февралю.
  **Готово:** сохранён отчёт с периодами, числом точек и источником признаков.
  Оценку по измеренной погоде явно отличать от качества прогноза по погодному API.
  **Отчёт:** [EVALUATION_REPORT.md](EVALUATION_REPORT.md), команда
  `python -m scripts.evaluate`; декабрьская разработка и январский тест,
  24/48 часов, MAE/RMSE/R² и baseline последней завершённой мощности в origin.
- [ ] **B4 / P2: пакетный февральский расчёт.** CLI scripts/replay.py уже готов; после A4 выполнить его для
  28 ежедневных origin × 2 турбины × 48 часов = 2 688 строк, включая выход
  горизонта в март. Проверять покрытие, дубликаты, порядок времени и происхождение.
  **Статус:** fixtures — 4 запуска / 192 строки; archive — 56 отсутствующих
  выпусков, полный результат заблокирован A4.
  **Готово:** одна команда создаёт результат и отчёт; пропущенные данные
  перечислены явно, формат сверяется с требованиями организаторов.
- [ ] **B5 / P2: оценка февраля при появлении истины.** Подключить фактическую
  выработку только для итогового сравнения после фиксации прогнозов.
  **Готово:** метрики по турбинам и горизонтам воспроизводимы. Пока истинных
  значений нет, задача заблокирована внешними данными, метрики не выдумываются.

При изменении CSV согласовать версию схемы с A/C и одновременно обновить
writer, reader, признаки обучения и тесты. Существующие имена столбцов не переводить.

## Разработчик C — русский frontend и объяснения

Владеет `frontend/`, `explanation.py` и тестами объяснений.

- [x] **C1 / P0: перевести весь пользовательский сценарий.** Заголовки, кнопки,
  выбор турбины/даты/горизонта, подсказки, таблицу, легенду графика, скачивания,
  сравнение, пустые состояния, загрузку, ошибки и accessibility-подписи.
  Свести строки в один модуль русского текста без тяжёлой системы локализации.
  **Готово:** основной экран и все ветки демонстрации на русском; в HTML
  установлен `lang="ru"`, даты/числа отображаются через `ru-RU` с явным
  часовым поясом турбины. UTC в API и машинном CSV остаётся неизменным.
- [x] **C2 / P0: объяснение и диалог по-русски.** Указать русский язык в
  инструкциях LLM, перевести расчётные ответы, предупреждения и подсказки
  вопросов. Проверить распознавание русских вопросов локальной логикой,
  включая «В какие шесть часов средняя мощность максимальна?» и минимум.
  Обновить версию промпта/ключ кеша после изменения языка.
  **Готово:** объяснение, ответ без ключа, ошибка LLM и ответ на неподдерживаемый
  вопрос русские; числа берутся из прогноза, вычисления выполняются кодом.
- [x] **C3 / P0: понятные обозначения.** Использовать «Сформировать прогноз»,
  «Скачать входной CSV», «Задать вопрос», «Демонстрационная погода»,
  «Координаты пользователя», «Расчётное объяснение — ИИ недоступен».
  Подписывать мощность как нормализованную, не обещать процент паспортной мощности.
  **Готово:** пользователь различает реальные данные, заглушки и источник объяснения.
- [x] **C4 / P0: завершить состояния интерфейса.** Проверить 24/48 часов,
  смену T1/T2, смену параметров во время запроса, повтор после ошибки, потерю
  forecast ID, долгий ответ LLM и скачивание CSV именно активного прогноза.
  **Готово:** числа появляются до объяснения, старые ответы не попадают в новый
  прогноз, недоступность LLM не ломает таблицу и график.
- [x] **C5 / P0: русская инструкция показа.** Обновить раздел демо в README
  с точными названиями кнопок и подготовить сценарий презентации на 2–3 минуты.
  **Готово:** другой разработчик проходит сценарий без устных пояснений автора.

- [ ] **C6 / P0: репетиция объединённого демо после A3.** Пройти выбор обеих
  турбин, 24/48 часов, график, таблицу, CSV/PNG/SVG, русский анализ и вопросы.
  Проверить отказ погодного API и LLM, смену параметров во время запроса,
  мобильный экран и явное отличие настоящей погоды от fixtures.
  **Готово:** записаны результаты проверки и обновлён сценарий показа;
  неподтверждённые возможности не заявляются на презентации.

## Результаты модели и команды

Обновление 23 сентября: реализовано обучение отдельного профиля на признаках
Open-Meteo ECMWF IFS. Команды `scripts.fetch_training_weather` и
`scripts.train_forecast --activate`, сравнения и остаточные ошибки описаны в
[FORECASTING_REPORT.md](FORECASTING_REPORT.md). B1: программная совместимость
источника/высот/CSV проверена; высота исходного датчика неизвестна. B3: выполнена
хронологическая проверка по погоде провайдера, но точность на реальных lead 24/48
остаётся неподтверждённой. A4/B4/B5 остаются открытыми.

```sh
python -m scripts.train --mode fixture
python -m scripts.smoke_model --models-dir artifacts/models
```

Первая команда обучает обе модели и создаёт январские отчёты в
`artifacts/training/T1_metrics.json` и `T2_metrics.json`, а также CSV с фактом
и прогнозом. `--skip-validation` пропускает только диагностику. Новые модели
сохраняются в `artifacts/models/<турбина>/<хэш>/`, активные — в `latest.json`.

| Январь, 738 часов на турбину | T1 | T2 |
|---|---:|---:|
| MAE | 0,02373 | 0,02592 |
| RMSE | 0,04802 | 0,06225 |

Это нормализованные доли и **измеренная погода**, не оценка будущего прогноза
погодного API. Встроенная в обучение январская диагностика использует baseline,
замороженный на весь январь. Отдельная
[хронологическая оценка](EVALUATION_REPORT.md) сравнивает модель с baseline,
обновляемым при каждом ежедневном origin для 24/48 часов. Не заявлять качество
на as-issued погоде по этим числам.

## Порядок параллельной работы

1. A и B согласуют погодные признаки и режим `archive`/возможный `live`;
   A и C — ошибки и DTO. Изменения границ фиксируются в CONTRACTS.md.
2. A выполняет A1–A3; B — B1 и готовый CLI B4 после получения архива; C — подготовку C6; перевод и редизайн уже интегрированы.
3. После интеграции погоды совместно проверить обе турбины, 24/48 часов,
   CSV, объяснение без LLM и поведение при ошибках. Обновить инструкцию показа.
4. A4 предоставляет архив → B4 выполняет реальный replay → B5 оценивает
   результат, если организаторы предоставят истину.

Каждый коммит — небольшой законченный шаг с ID задачи; регулярно коммитить
работающий результат. Общие файлы и зависимости меняет согласованный владелец.
Не добавлять авторизацию, БД, очереди, иерархию агентов или второго LLM-провайдера
до закрытия обязательного сценария.

## Общая приёмка демо

- [x] После объединения dev-a, модели и редизайна: 155 тестов и сборка React прошли.
  При проверке старых локальных артефактов sklearn предупреждает об именах
  признаков; ошибок тестов нет. После новых изменений проверки повторить.
- [x] На интеграции модели: 8 CSV-сценариев T1/T2 × два origin × 24/48 часов прошли.
- [ ] Весь пользовательский сценарий, анализ, вопросы и ошибки на русском.
- [ ] Проверены успех, отсутствие ключа, ошибка погоды, ошибка LLM и смена параметров.
- [ ] Видно, какая погода использована, реальные ли координаты и кто создал объяснение.
- [ ] README соответствует текущему запуску и оставшимся ограничениям.
- [ ] Ключи не попали в Git/frontend; автоматические тесты не тратят API-кредиты.
- [ ] Проведена репетиция с сохранённой демонстрационной погодой на случай
  недоступности API; зависимость карты от сети учтена, выбор турбины доступен списком.

Новый платный smoke test не входит в автоматическую приёмку: ранее разрешённый
один живой вызов уже выполнен. Остальные проверки объяснений используют моки.

## Устойчивость — выполнено

- [x] Пятиминутный кеш погоды, ограничение размера, время получения и явное обновление.
- [x] Один повтор при пересечении границы UTC-часа, без подмены происхождения.
- [x] Полное удаление baseline из пользовательских JSON/CSV и объяснений.
- [x] Локальное сохранение прогнозов с контрольной суммой и ограничением хранения.
- [x] Проверены CSV и объяснение после настоящего перезапуска сервера.
- [x] Браузерная проверка 390/1280 px с моками: оба горизонта, выгрузки,
  обновление, ошибки провайдера, запоздалые ответы и вопрос к активному forecast ID.
- [x] Реальная погода: получение → кеш → принудительное обновление → модель.
- [x] CLI февральского replay, проверка полноты и явный отчёт о блокерах.
- [x] TEAM_WORKFLOW.md: обновление от main, владельцы контрактов, проверки перед merge.
- [ ] Полный проверенный февральский архив (A4), реальный replay (B4),
  итоговые метрики после получения истины (B5). Ретроспективные обучающие данные
  новой модели не заменяют доказательства исторической доступности прогноза.
